[package]
name = "thalyx-boot-protocol"
description = "Versioned hand-off contract between the Thalyx UEFI loader and the kernel."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"
missing_docs = "warn"
unsafe_code = "deny"
