---
id: VAL-002
kind: plan
status: planned
---
# Experimentos y criterios de evidencia

## Obligaciones de implementación

EXP-01 está ejecutado; EXP-02, EXP-03, EXP-04, EXP-05 y EXP-06 lo están **en su alcance K3**, que es multiprocesador con un dispositivo virtio moderno y sin estado durable, sobre un emulador y **sin unidad de remapeo programada**. EXP-07 y EXP-08 están ejecutados **en su alcance K4**, y EXP-09 lo está **en su parte K4**: sobre un medio real conducido por el driver del invitado, con la supresión hecha en el invitado y no por el emulador. EXP-10 está ejecutado **en su alcance K5**: un modelo pequeño, un hilo de cómputo y sin comprobación de tipos. EXP-11 está ejecutado: la parte K5 —perfiles declarados, rechazo del insuficiente y diferencias registradas— y la parte K6 —la misma pregunta al motor sobre los dos backends con la respuesta contrastada byte a byte en cada repetición—. EXP-12 está ejecutado **en su alcance K6**: sobre KVM, en la máquina de desarrollo, con seis rondas emparejadas y las conclusiones que la equivalencia declarada permite. Nada está ejecutado sobre hardware físico. Los modelos de diseño que ya se pueden ejecutar tienen nombres MODEL-01/02 y se reportan por separado.

| ID | Experimento | Criterio de corrección | Fase |
|---|---|---|---|
| EXP-01 | Arranque y dos dominios adversarios; faults y FP. | Primer ring 3 después de protección; probes ilegales fallan sin dañar otro dominio; temporizador preempta un loop. | K1–K2. **Ejecutado**: [K1](../evidence/k1-protected-boot.md) y [K2](../evidence/k2-objects-authority-work.md). |
| EXP-02 | Caps: derivar, mover, copiar, expirar, reciclar slots y reiniciar. | Ninguna autoridad amplificada ni handle antiguo reinterpretado; operaciones cerradas rechazadas. | K2–K3. **Parte K3 ejecutada**, con cuatro procesadores y llamadas concurrentes en vuelo; sin reinicio con handles persistidos: [K2](../evidence/k2-objects-authority-work.md), [K3](../evidence/k3-smp-devices.md). |
| EXP-03 | Cierre concurrente con RPC, servidor muerto y operaciones recibidas. | Barrera separada de drenaje, contadores no falsamente cero, resultado posterior admitido solo cuando corresponde. | K2–K4. **Parte K3 ejecutada**, con llamantes en procesadores distintos; sin servidor muerto ni estado durable: [K2](../evidence/k2-objects-authority-work.md), [K3](../evidence/k3-smp-devices.md). |
| EXP-04 | Fan-out, CPU SMP, memoria, tickets y mantenimiento. | Conservación de cargos y presupuesto agregado, deuda visible, recuperación conserva sus reservas. | K2–K3. **Parte K3 ejecutada**: reserva agregada antes del despacho en cuatro procesadores, techo que rechaza, deuda arrastrada y exceso de ejecución medido contra la latencia de preempción real. Sin hardware físico: [K3](../evidence/k3-smp-devices.md). |
| EXP-05 | Alias y TLB remotos, DMA tardío, reset, zeroing y estado FP. | No reutilización peligrosa; sello solo después de retirada; perfil DMA honesto. | K3. **Ejecutado salvo el DMA tardío**: invalidación acusada por los cuatro procesadores, sello publicado contra un escritor vivo, reclamación diferida por generación, estado FP que viaja con la migración, reset confirmado leyendo el transporte, y perfil débil declarado con el fuerte rechazado. **Nada impide físicamente una escritura del dispositivo en esta plataforma**: [K3](../evidence/k3-smp-devices.md). |
| EXP-06 | IPC malformado y agotamiento de colas/handles/logs. | Sin efectos parciales de transferencia, sin allocation ilimitado y cierre disponible. | K2–K3. **Parte K3 ejecutada**, con cuatro procesadores y varios llamantes; sin varios servidores concurrentes: [K2](../evidence/k2-objects-authority-work.md), [K3](../evidence/k3-smp-devices.md). |
| EXP-07 | Versiones, herramientas que cambian inputs, CAS y ABA. | Validación ligada a versión; conflicto correcto; no publicación con permiso de escritura privada. | K4. **Ejecutado**: evidencia que nombra otra versión rechazada con `VALIDATION_MISMATCH` antes de mirar siquiera la expectativa, expectativa vieja rechazada con `GENERATION_STALE`, identidad reutilizada con otros inputs rechazada con `CONFLICT`, hueco de secuencia rechazado, un lector que puede preparar contenido rechazado con `FORBIDDEN` al publicar, y dos publicadores compitiendo por la misma transición con una sola ganadora. [K4](../evidence/k4-durable-state.md). |
| EXP-08 | Caídas en cada escritura/flush/respuesta, reintentos y compactación. | ACK recuperable; raíz/política/recibo coherentes; sin doble efecto por retry ni liberación temprana. | K4. **Ejecutado**: once cortes distintos, uno por cada punto donde una publicación puede cortarse, cada uno seguido de un arranque sobre lo que el corte dejó; un prepare sin resolver recuperado como abort, un commit durable adoptado sin su checkpoint, una respuesta perdida contestada desde el resultado durable, y ninguna identidad de petición con dos generaciones en ningún medio de la matriz. [K4](../evidence/k4-durable-state.md). |
| EXP-09 | Evidencia incompleta, remoto incierto, saturación y servicio de control perdido. | No falsa auditabilidad, rollback ni causalidad; diagnóstico nombra el límite. | K4–K5. **Parte K4 ejecutada**: evidencia que no nombra estos inputs rechazada, un broker que contesta `UNKNOWN` y un servicio que lo registra como resultado, saturación del área de preparación resuelta por barrido en vez de por agotamiento, y el plano de control perdido —el log lleno rechaza las admisiones que sus recibos cubrirían en vez de perderlos, y el servicio se declara `UNAVAILABLE` en vez de llamar dañado a un almacén intacto—. Sin efecto remoto real ni servicio de control distribuido: [K4](../evidence/k4-durable-state.md). |
| EXP-10 | Thalyx completo en una vertical nativa con motor y herramienta reales. | Misma semántica esperada y fallos honestos; residencia/consumo medidos, ninguna dependencia oculta en host. | K5. **Ejecutado en su alcance K5**: la vertical entera —contexto, programa en QuickJS real, herramienta real en dominio propio, motor residente real, publicación condicionada, evidencia— con las completaciones iguales byte a byte a las del motor de Thalyx en Linux; residencia y petición cobradas por el kernel a ámbitos distintos; y la matriz adversaria: dos rivales sobre un motor sin fuga, cierre durante el servicio residente con el motor intacto, y tres publicaciones que fallan y se recuperan. El anfitrión solo construye, escribe la directiva y contesta la referencia, etiquetada como ejecución del anfitrión. Modelo pequeño, un hilo de cómputo, sin comprobación de tipos: [K5](../evidence/k5-thalyx-port.md). |
| EXP-11 | Backends Linux y nativo con perfiles equivalentes. | Rechazo del perfil insuficiente y fixtures de contrato compartidos; diferencias registradas. | K5–K6. **Ejecutado**. Parte K6: el motor nativo y el de Thalyx en un invitado Linux de la misma máquina contestan las tres preguntas de la fixture con la respuesta de la referencia en las 54 entradas de la campaña, y `engine.infer` se compara como equivalente: nativo un 10 % más lento, con intervalo cerrado. [K6](../evidence/k6-comparison-hardening.md). Parte K5: la pregunta al motor sale de una fixture del esquema para el programa nativo, la referencia Linux y la puerta, y las completaciones coinciden byte a byte; el backend nativo declara su perfil en cada versión que publica y cada rasgo se contrasta con un registro del kernel o del motor; una comprobación que el perfil no tiene —`Check::Rust`— se rechaza con `NO_SUCH_TOOL` y `not_proven`, exigido por el propio programa; el perfil de Linux, leído de la revisión fijada y no ejecutado, está en la misma fixture y las diferencias son el detalle del criterio. Sin medidas: [K5](../evidence/k5-thalyx-port.md). |
| EXP-12 | Rendimiento y escalabilidad con controles emparejados. | Muestras, condiciones e intervalos; ninguna conclusión de velocidad sin equivalencia. | K6. **Ejecutado en su alcance K6**: diecisiete benchmarks de una fuente C sobre este kernel y sobre Linux —con y sin mitigaciones— como invitados de la misma máquina QEMU/KVM, seis rondas emparejadas, bootstrap sobre rondas, y el veredicto de cada comparación acotado por la equivalencia que el esquema declaró antes de correr. Lo que dice: una llamada IPC con sus garantías cuesta 0,41 de un socket con credenciales; mapear una página cuesta 4,7 veces `mmap` y 64 páginas 0,45; el despertar es 4,4 veces más lento por el plazo de gracia elegido; el cómputo escala 3,95× en cuatro procesadores y el IPC **no** escala —0,15 de Linux con cuatro pares, por el cerrojo único—; la inferencia del mismo modelo con la misma respuesta es un 10 % más lenta. Ocho cuellos encontrados midiendo y arreglados con cifra de antes y después. Nada sobre hardware físico. [K6](../evidence/k6-comparison-hardening.md). |

## Cómo producir un resultado útil

Cada informe identifica pregunta, hipótesis rival, commits de kernel/consumidor, build, hardware o emulador, configuración, inputs y seed. Incluye comando, salida cruda o artefacto con hash, observación, interpretación y límites.

Los resultados posibles son `PASS`, `FAIL`, `NOT_RUN`, `INCONCLUSIVE` y `UNSUPPORTED_PROFILE`. Un ensayo que no pudo ejecutarse no pasa. Un assert ausente en un smoke test no prueba una propiedad adversaria.

Las pruebas de recursos deben incluir gasto posterior al cierre y el del servicio, no solo duración del cliente. Las de persistencia deben distinguir caída del proceso, caída de la VM y pérdida de energía/dispositivo. Las de aislamiento deben incluir vías alternativas de acceso.

## Modelos ejecutables incluidos

MODEL-01 explora un espacio finito de delegación, dos invocaciones, admisión de efecto, barrera y resolución. Busca deliberadamente el contraejemplo a «nunca hay commit después de fence», a la vez que comprueba que no se admiten efectos nuevos después de la barrera y no se retira un ámbito con tickets pendientes.

La variante rival que reutiliza un check anterior sin sincronizar admisión debe producir la secuencia check → fence → admisión indebida. Así se contrasta el mecanismo conjunto con una separación tentadora pero incorrecta.

MODEL-02 enumera cortes y subconjuntos de escrituras persistidas de un protocolo abstracto de publicación. Comprueba dependencias antes de commit y supervivencia de un ACK; variantes incorrectas deben producir contraejemplos. Incluye una demostración mínima de ABA en comparación por contenido.

No incluyen kernel Rust, instrucciones CPU, hardware, checksums reales, múltiples almacenes, toda la lógica de dedupe o un disco real. Su valor es detectar contradicciones del contrato temprano, no certificar implementación. [Código y resultados](../../research/models/README.md).

## Qué refutaría la arquitectura práctica

Coste de IPC que domina workloads relevantes; crecimiento no acotado de metadatos; limpieza que depende sistemáticamente de matar servicios compartidos; port de herramientas que requiere entregar autoridad ambiental a todo el sistema; imposibilidad de reproducir perfiles entre backends; una propiedad esencial que no encuentra mecanismo bajo fallos.

Esos resultados obligan a revisar el mecanismo o la frontera afectados. No se esconden mediante un benchmark de otra carga ni se interpreta la ambición del proyecto como evidencia.
