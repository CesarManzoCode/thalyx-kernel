[workspace]
resolver = "3"
members = [
    "abi",
    "boot/protocol",
    "boot/uefi",
    "kernel",
    "user/rt",
    "user/worker",
    "user/trespasser",
    "user/wxprobe",
    "user/k2super",
    "user/k2server",
    "user/k2client",
    "user/k3super",
    "user/k3worker",
    "user/k3driver",
    "user/k4fmt",
    "user/k4disk",
    "user/k4store",
    "user/k4client",
    "user/k4super",
    "user/k5pkg",
    "user/k5super",
    "user/k5work",
    "user/k6super",
]

[workspace.package]
version = "0.7.0"
edition = "2024"
rust-version = "1.98.1"
license = "NOASSERTION"
publish = false

[workspace.dependencies]
thalyx-abi = { path = "abi" }
thalyx-boot-protocol = { path = "boot/protocol" }
thalyx-user-rt = { path = "user/rt" }
thalyx-user-k4fmt = { path = "user/k4fmt" }
thalyx-user-k5pkg = { path = "user/k5pkg" }

# Bare-metal images must not unwind: there is no unwinder and no runtime below
# the kernel. `panic = "abort"` is also the target default for both
# x86_64-unknown-none and x86_64-unknown-uefi; stating it keeps a profile
# override from silently reintroducing landing pads.
[profile.dev]
panic = "abort"
overflow-checks = true
debug = true

[profile.release]
panic = "abort"
overflow-checks = true
debug = true
lto = false
codegen-units = 1
